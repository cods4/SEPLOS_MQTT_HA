# Home Assistant creates the Seplos sensors by MQTT discovery.
# Remove any previously pasted Seplos mqtt: and template: sensor blocks from
# configuration.yaml so they do not collide with the discovered entities.
#
# Optional dashboard: lovelace.yaml
